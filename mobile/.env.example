# Used only while Expo resolves app.config.ts for native builds.
NAVER_MAP_CLIENT_ID=

# Public runtime value. Android Emulator reaches the host through 10.0.2.2.
# Use your computer's LAN IP for a physical device.
EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:8080
